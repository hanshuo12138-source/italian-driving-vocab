# IMAGE_SOURCE_AUDIT.md

## 审查范围

本次只审查 `words.csv` 的 `image` 列。

未执行的操作：

- 未下载任何图片。
- 未删除任何图片。
- 未修改 `words.csv`。
- 未修改 `app.py`。
- 未修改账户逻辑或其他业务代码。

## 分类标准

- A：远程 URL 外链，例如 `https://...`
- B：本地图片路径，例如 `assets/...` 或 `images/...`
- C：空值
- D：格式异常

## 统计结果

| 项目 | 数量 |
| --- | ---: |
| 总词条数 | 484 |
| `image` 为空 | 50 |
| `image` 为远程 URL | 434 |
| `image` 为本地路径 | 0 |
| `image` 格式异常 | 0 |

## 远程 URL 来源统计

| 域名 | 数量 |
| --- | ---: |
| `www.rmastri.it` | 434 |

当前所有远程图片都来自同一个域名，路径主要形如：

```text
https://www.rmastri.it/42/immagini/s1.gif
https://www.rmastri.it/42/immagini/s2.gif
https://www.rmastri.it/42/immagini/s3.gif
```

也就是：

```text
https://www.rmastri.it/42/immagini/s{编号}.gif
```

## 本地图片路径检查

本次未发现本地图片路径。

因此不存在需要检查真实文件是否存在的本地图片记录。

## 格式异常检查

本次未发现格式异常的 `image` 值。

没有发现以下情况：

- 非 URL 且非本地路径的异常字符串。
- 无法解析的图片路径。
- 混乱格式。

## 当前图片使用方式判断

当前图片主要是 **远程 URL 外链**，不是本地复制。

结论：

- 当前项目没有把这些交通标志/图形图片复制到本地项目文件夹。
- `words.csv` 只是保存远程图片链接。
- 应用运行时会通过浏览器加载这些外链图片。

## 来源最多的记录

来源最多的是：

```text
www.rmastri.it
```

数量：

```text
434
```

这些记录主要是交通标志与图形类词条，包括：

- 危险标志
- 优先权标志
- 禁止标志
- 强制标志
- 辅助牌
- 指示标志
- 临时与施工标志
- 交警手势
- 路面标线
- 路口优先顺序
- 灯光与仪表符号

## 风险记录

### 版权/授权风险

所有 434 条远程图片都需要在正式商业化前人工确认版权和授权。

原因：

- 当前只是远程引用公开网页资源。
- 公开可访问不等于一定允许商业用途。
- 远程外链是否允许长期引用，需要查看原站说明、版权声明或联系来源方确认。

### 外链稳定性风险

所有 434 条远程图片都依赖 `www.rmastri.it`。

如果该网站：

- 改变图片路径；
- 阻止外链；
- 删除资源；
- 服务器不可用；
- 更改访问策略；

应用中的交通标志图片就可能无法显示。

### 商业化风险

如果未来应用用于收费、广告、会员、课程销售或其他商业用途，这 434 条远程图片都应先做版权确认。

在版权未确认前，不建议：

- 将远程图片复制到本地；
- 打包进商业产品；
- 作为付费卖点宣传；
- 移除来源说明；
- 暗示这些图片属于本项目原创资源。

## 需要人工确认版权的记录

需要人工确认版权的记录范围：

```text
所有 image 域名为 www.rmastri.it 的 434 条记录
```

建议确认内容：

1. 是否允许在第三方学习工具中展示。
2. 是否允许商业用途。
3. 是否允许远程外链。
4. 是否需要标注来源。
5. 是否允许复制到本地。
6. 是否有更合适的官方图标来源。

## 建议后续操作

1. 短期测试阶段可以继续使用远程外链，但应保留来源说明。
2. 商业化前完成图片版权来源自查。
3. 如果授权不明确，优先考虑改用：
   - 官方允许使用的交通标志资源；
   - 自制矢量图；
   - 已授权的图片包；
   - 只保留文字释义，不显示图片。
4. 不要在版权未确认前把这些图片下载到本地项目。
5. 后续如果新增本地图片，应先记录来源、授权和用途。

## 总结

当前 `words.csv` 的图片状态整体清晰：

- 50 条没有图片。
- 434 条使用远程 URL 外链。
- 0 条使用本地路径。
- 0 条格式异常。

主要风险不是格式问题，而是 **版权授权和外链稳定性**。
