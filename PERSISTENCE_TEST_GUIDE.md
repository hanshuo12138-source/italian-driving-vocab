# PERSISTENCE_TEST_GUIDE.md

## 目标

测试 `data/app.db` 在本地重启或 Streamlit Community Cloud 重新部署后是否仍然保留。

注意：即使测试中数据仍然存在，也只说明当前环境短期可用，不代表长期可靠。

## A. 本地测试

1. 本地运行应用。
2. 创建一个测试账号。
3. 登录测试账号。
4. 收藏几个词，或添加生词、错词。
5. 进入管理员后台。
6. 在“数据持久性测试”区域创建一条 persistence marker。
7. 记录页面显示的数据库路径、文件大小、服务器时间。
8. 关闭应用。
9. 重新运行应用。
10. 使用同一个测试账号登录。
11. 检查收藏、学习记录是否还在。
12. 进入管理员后台，检查刚才创建的 persistence marker 是否还在。

## B. Streamlit Cloud 测试

1. 打开线上应用。
2. 在线创建一个测试账号。
3. 登录测试账号。
4. 收藏几个词，或添加生词、错词。
5. 进入管理员后台。
6. 在“数据持久性测试”区域创建一条 persistence marker。
7. 记录 marker 的 key、value 和创建时间。
8. 在 GitHub 提交一个小改动，触发 Streamlit Community Cloud 重新部署。
9. 等待应用重新启动。
10. 重新打开线上应用。
11. 使用同一个测试账号登录。
12. 检查收藏、学习记录是否还在。
13. 进入管理员后台，检查 persistence marker 是否还在。

## C. 结论判断

如果数据仍在：

- 说明当前环境短期可用。
- 但 Streamlit Community Cloud 本地文件仍不保证长期持久。
- 后续商业化前仍建议迁移到外部数据库。

如果数据丢失：

- 说明当前部署环境不适合保存正式用户数据。
- 应尽快迁移到外部数据库，例如 Supabase 或 PostgreSQL。

## 建议

上线测试阶段可以继续观察 `data/app.db` 的稳定性。

正式商业化、会员、付费、广告投放或用户增长前，应优先迁移到外部数据库。
