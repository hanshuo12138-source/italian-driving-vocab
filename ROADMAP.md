# ROADMAP.md

## 1. 域名和品牌入口

目标：让应用有更清晰的品牌入口，方便分享和后续商业化。

建议：

- 先确定应用名称和中文品牌名。
- 购买一个简单易记的域名。
- 将域名跳转到 Streamlit 应用。
- 后续可以制作首页、隐私说明和联系方式。

## 2. SQLite 数据库迁移

目标：从 JSON 文件逐步迁移到 SQLite，降低数据丢失风险，方便后续管理。

优先迁移：

- 用户账户。
- 收藏。
- 生词本。
- 已掌握状态。
- 闪卡学习记录。

原则：

- 先保留 JSON 作为备份。
- 写迁移脚本，不要手动删除旧数据。
- 迁移完成后再逐步减少 JSON 依赖。

## 3. 图片版权来源自查

目标：确认交通标志和图形图片的使用方式是否适合公开和商业化。

需要检查：

- 图片来源。
- 是否允许远程引用。
- 是否允许商业用途。
- 是否需要标注来源。
- 是否应该改用官方或自制图形资源。

原则：

- 来源不明确的图片不要本地复制。
- 商业化前必须完成版权自查。

## 4. 管理员权限安全检查

目标：为未来管理员功能打基础，避免只靠隐藏按钮保护后台。

要求：

- 管理员账户必须有明确权限字段。
- 后台页面必须检查权限。
- 不能只在 UI 上隐藏按钮。
- 上传、删除、修改词库都必须验证管理员身份。

## 5. 管理员后台上传词库

目标：减少每次修改词库都要上传 GitHub 的麻烦。

功能设想：

- 管理员登录后进入后台。
- 上传新的 `words.csv`。
- 检查字段格式。
- 预览新增、修改、删除数量。
- 确认后写入数据库或词库文件。
- 支持导出当前词库。

优先级：高。

## 6. 用户行为统计

目标：了解用户是否真的在使用，哪些功能最有价值。

可统计：

- 注册用户数。
- 活跃用户数。
- 每日学习次数。
- 最常搜索的词。
- 最常加入生词本的词。
- 闪卡完成数量。
- 章节完成进度。

注意：

- 不要收集不必要的隐私数据。
- 后续需要隐私说明。

## 7. 多语言界面

目标：从中文用户扩展到更多移民用户。

可扩展语言：

- 中文。
- 英文。
- 西班牙语。
- 法语。
- 阿拉伯语。

建议：

- 先抽离界面文本。
- 再扩展词库释义字段。
- 不要一开始就做过大改动。

## 8. 错题本

目标：记录用户不会的词和测验错误。

功能设想：

- 闪卡中“不熟”的词自动进入错题/生词复习。
- 测验答错的词进入错题本。
- 用户可以手动移出。
- 错题本可以按章节过滤。

## 9. 智能复习

目标：根据用户学习情况安排复习。

功能设想：

- 根据认识/不熟次数调整复习频率。
- 长期未复习的词自动提醒。
- 生词优先出现。
- 已掌握词降低出现频率。

建议：

- 先做简单规则，不要一开始接复杂 AI。

## 10. 模拟考试

目标：从词汇学习扩展到考试训练。

功能设想：

- 按章节生成小测验。
- 支持判断题或选择题。
- 显示正确率。
- 记录错题。
- 后续再做完整模拟考试。

注意：

- 不要直接复制版权不明确的完整题库原文。
- 可以基于词汇和概念生成自有练习题。

## 11. 广告位系统

目标：为未来商业化预留低成本变现方式。

功能设想：

- 首页广告位。
- 章节页广告位。
- 学习完成页广告位。
- 后台配置广告内容。

注意：

- 广告不要影响学习体验。
- 如果面向商业化，需要明确隐私和广告说明。

## 12. 未来迁移到 Supabase/PostgreSQL

目标：当用户量增长后，迁移到更稳定的云数据库。

适合迁移的情况：

- 用户明显增多。
- SQLite 不方便部署或备份。
- 需要多设备稳定同步。
- 需要管理员后台。
- 需要支付、会员、权限系统。

建议路线：

1. 当前 JSON。
2. 迁移到 SQLite。
3. 稳定后再迁移到 Supabase 或 PostgreSQL。

原则：

- 不要过早迁移。
- 先验证产品需求。
- 迁移前做好数据备份和迁移脚本。
